SUPABASE_URL="https://your-project-id.supabase.co"
SUPABASE_KEY="your-service-role-key-here"

GEMINI_KEY="your-gemini-key"

TAVILY_KEY=

# Frontend (Expo) vars live in frontend/.env — see frontend/.env.example.
# MAPBOX_TOKEN is the Mapbox public token, read by the app as EXPO_PUBLIC_MAPBOX_TOKEN.
MAPBOX_TOKEN="pk.your-mapbox-public-token"
